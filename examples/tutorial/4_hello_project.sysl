# The fantastic Hello World greeting system.

HelloService:
    GetGreeting(userId):
        ...

HelloApp:
    Greet:
        HelloService <- GetGreeting

HelloProject [appfmt="%(appname)"]:
    Greeting:
        HelloApp
        HelloService
        HelloApp <- Greet
